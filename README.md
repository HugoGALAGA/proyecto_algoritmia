Navegacion tipo waze a travez del algoritmo de dijkstra

ruta mas rapida entre dos puntos
ruta con una parada antes del destino
ruta evitando una parada
ruta con trafico

git clone https://github.com/HugoGALAGA/proyecto_algoritmia
cd carpeta

pip install networkx matplotlib (libreria)

ejecutar en la carpeta python main.py